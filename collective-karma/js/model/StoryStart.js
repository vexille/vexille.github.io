define([
    'parse'],
        function (Parse) {
            // story : pointer to the story
            var StoryStart = Parse.Object.extend("StoryStart");
            return StoryStart;
        });